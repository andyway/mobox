extends layouts/default

block sidebar
  header.header.bg-dark.bg-gradient(ng-include="'views/sidebar.html'" ng-controller="root.sidebar" ng-show="currentUser._id")

block content
  section.scrollable.wrapper
    .row(data-ui-view)
  script(type="text/javascript").
    window.user = !{user};
